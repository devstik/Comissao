
# Atualizações aplicadas

- **Login simplificado**: apenas Usuário e Senha (padrão `admin` / `123456`).
- **Conexão**: usa `config.py` por baixo dos panos (não aparece no login).
- **Tema**: Dark Purple aplicado (palette + stylesheet).
- **UI moderna**: botões com estilo "bootstrap-like" (hover/pressed, bordas arredondadas).
- **Label amigável**: onde aparecia *NmLot*, a interface agora usa **Cor** (e placeholder "Cor (ex.: Preto, Bronze 179)").
- Demais funcionalidades de consulta/extrato permanecem.

Para executar:
```bash
python main.py
```
